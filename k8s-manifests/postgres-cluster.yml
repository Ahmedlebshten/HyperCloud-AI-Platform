apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: guestbook-db
spec:
  instances: 3
  imageName: ghcr.io/cloudnative-pg/postgresql:15
  storage:
    size: 2Gi
  bootstrap:
    initdb:
      database: guestbook
      owner: user_admin